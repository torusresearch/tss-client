{
  "name": "@toruslabs/tss-lib",
  "version": "3.0.0",
  "description": "Packaged WASM file for the DKLS threshold signing client library.",
  "main": "dist/tssLib.cjs.js",
  "unpkg": "dist/tssLib.umd.min.js",
  "jsdelivr": "dist/tssLib.umd.min.js",
  "types": "dist/types/dkls.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "watch": "torus-scripts start",
    "build": "torus-scripts build",
    "prepack": "npm run build",
    "release": "torus-scripts release"
  },
  "devDependencies": {
    "ts-loader": "^9.4.2"
  }
}
